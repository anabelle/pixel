{
  "name": "narrative-correlator-bridge",
  "version": "1.0.0",
  "description": "Data pipeline that extracts narratives from agent memory and economic events from API, then posts to correlator service",
  "main": "narrative-correlator-bridge.ts",
  "type": "module",
  "scripts": {
    "start": "bun run narrative-correlator-bridge.ts",
    "test": "bun test narrative-correlator-bridge.test.ts"
  },
  "dependencies": {
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0"
  }
}
